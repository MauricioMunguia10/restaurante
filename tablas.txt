create database restaurante;
use restaurante;

create table sesiones(
    id int not null auto_increment primary key,
    correo varchar(30) not null,
    contrasena varchar(30) not null,
    rol int not null default 0
); 

insert into sesiones values("","mauricio@gmail.com","1234",1);
insert into sesiones values("","munguia@gmail.com","1234","");

create table empleado(
    id int not null auto_increment primary key,
    nombre varchar(30),
    p_apellido varchar(30),
    s_apellido varchar(30),
    telefono int,
    email varchar(20),
    direccion varchar(30),
    puesto varchar(20),
    rol int,
    salario int
);

insert into empleado values("","Mauricio","Munguia","Garcia",5517287218,"mau@gmail.com","casa","mesero",0,2500);

create table orden(
    id int not null auto_increment primary key,
    sucursal varchar(30),
    empleado varchar(20),
    comida varchar(50),
    total int
);

insert into orden values ("","Nicolas Romero","Mauricio","pozole,pasta,ensalada,coca,tiramisu",1782);

create table comida(
    id int not null auto_increment primary key,
    nombre varchar(20),
    cantidad varchar(20),
    precio int
);

insert into comida values ("","pozole","450 gr", 78);